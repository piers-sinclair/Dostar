# Copy this file to .env.local for local development (gitignored)

# URL of the backend API (Vite dev server proxies are not used — this must be the full origin)
VITE_API_BASE_URL=http://localhost:5000
