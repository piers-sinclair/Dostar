services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: dostar
      POSTGRES_PASSWORD: dostar
      POSTGRES_DB: dostar
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
